JWT is a token generates a unique token for the user when they login and signup which help user to browse the application with just single login.

bcrypt - bcrypt is a library for encode and decode the password 